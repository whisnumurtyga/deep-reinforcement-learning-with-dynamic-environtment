[NEAT]
# Kriteria kebugaran terbaik: dalam hal ini, nilai maksimum
fitness_criterion     = max

# Ambang batas kebugaran, jika tercapai oleh suatu organisme, akan menghentikan evolusi
fitness_threshold     = 100000000

# Jumlah organisme (individu) dalam satu generasi atau populasi
pop_size              = 30

# Jika populasi punah (tidak ada organisme yang hidup), apakah akan mereset populasi atau tidak
reset_on_extinction   = True

[DefaultGenome]
# Opsi aktivasi node default
activation_default      = tanh

# Tingkat mutasi untuk aktivasi node
activation_mutate_rate  = 0.01

# Opsi aktivasi yang mungkin
activation_options      = tanh

# Opsi agregasi node default
aggregation_default     = sum

# Tingkat mutasi untuk agregasi node
aggregation_mutate_rate = 0.01

# Opsi agregasi yang mungkin
aggregation_options     = sum

# Opsi inisialisasi nilai bias node
bias_init_mean          = 0.0
bias_init_stdev         = 1.0
bias_max_value          = 30.0
bias_min_value          = -30.0
bias_mutate_power       = 0.5
bias_mutate_rate        = 0.7
bias_replace_rate       = 0.1

# Opsi kesesuaian genom untuk simpul yang tidak cocok
compatibility_disjoint_coefficient = 1.0

# Opsi kesesuaian genom untuk bobot yang tidak cocok
compatibility_weight_coefficient   = 0.5

# Tingkat mutasi untuk menambahkan koneksi antar node
conn_add_prob           = 0.5

# Tingkat mutasi untuk menghapus koneksi antar node
conn_delete_prob        = 0.5

# Opsi default untuk status koneksi antar node
enabled_default         = True

# Tingkat mutasi untuk mengubah status koneksi antar node
enabled_mutate_rate     = 0.01

# Jenis hubungan koneksi (feed-forward atau tidak)
feed_forward            = True

# Jenis koneksi antar node saat pertama kali diinisialisasi
initial_connection      = full

# Tingkat mutasi untuk menambahkan simpul
node_add_prob           = 0.2

# Tingkat mutasi untuk menghapus simpul
node_delete_prob        = 0.2

# Jumlah simpul tersembunyi dalam jaringan
num_hidden              = 0

# Jumlah input yang dimasukkan ke dalam jaringan
num_inputs              = 5

# Jumlah output yang dihasilkan oleh jaringan
num_outputs             = 4

# Opsi inisialisasi nilai respons simpul
response_init_mean      = 1.0
response_init_stdev     = 0.0
response_max_value      = 30.0
response_min_value      = -30.0
response_mutate_power   = 0.0
response_mutate_rate    = 0.0
response_replace_rate   = 0.0

# Opsi inisialisasi bobot koneksi antar node
weight_init_mean        = 0.0
weight_init_stdev       = 1.0
weight_max_value        = 30
weight_min_value        = -30
weight_mutate_power     = 0.5
weight_mutate_rate      = 0.8
weight_replace_rate     = 0.1

[DefaultSpeciesSet]
# Ambang batas kesesuaian untuk menentukan spesies yang sama
compatibility_threshold = 2.0

[DefaultStagnation]
# Fungsi evaluasi kebugaran spesies
species_fitness_func = max

# Jumlah generasi di mana stagnasi diizinkan sebelum menganggap bahwa spesies tersebut telah mandek
max_stagnation       = 20

# Jumlah spesies elit yang akan dibiarkan tetap stabil meskipun stagnasi
species_elitism      = 2

[DefaultReproduction]
# Jumlah individu terbaik yang dibiarkan tetap stabil di setiap generasi
elitism            = 3

# Ambang batas untuk memutuskan apakah organisme akan tetap hidup atau tidak
survival_threshold = 0.2
